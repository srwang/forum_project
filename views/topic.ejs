<!DOCTYPE html>
<html>
	<link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.6.0/pure-min.css">
	<link rel="stylesheet" href="http://localhost:3000/main.css">
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
<body>
	<header>
		<h1>Dream Lucid</h1>
		<p id="header-text-small">Architecting the world of REM connections</p> 
	</header>
	<div class="main-content">
		<h1><%=topic.title%></h1>
		<h3><%=topic.summary%></h3>
		<div class="wrapper topic-wrapper">		
			<p><%=topic.body%></p>
			<p>Posted by USER at <%=topic.created_at%></p>
		</div>
		<button class="reply-button">Leave Comment</button>
		<form class="reply-form" action='/dreamlucid/topic/<%=topic.id%>/comment' method='POST'>
			<input class="reply" type="text" name="replyBody" placeholder="Comment on this post">
			<button class="reply-submit">Submit</button>
		</form>
		<%for(i=0; i<comments.length; i++){%>
			<div class="wrapper comment-wrapper">
				<form action='/dreamlucid/topic/<%=topic.id%>/comment/<%=comments[i].id%>' method='POST'>
					<button>+</button>
				</form>
				<p><%=comments[i].like_count%> Likes</p>
				<p><%=comments[i].body%></p>
				<%if (comments[i].created_at !== comments[i].updated_at){%>
					<p>Edited by USER at <%=comments[i].updated_at%></p>
				<%}%>
				<p>Posted by USER at <%=comments[i].created_at%></p>
			</div>
		<form action='/dreamlucid/topic/<%=topic.id%>/comment/<%=comments[i].id%>/edit' method='GET'>
			<button>Edit</button>
		</form>
<!-- 			<button class="comment-button>" id="button-<%=i%>">Leave Reply</button>
			<form class="comment-form>" id= "form<%=i%>" action='/dreamlucid/topic/<%=topic.id%>/comment' method='POST'>
				<input class="comment" type="text" name="commentBody" placeholder="Comment on this comment">
				<button class="comment-submit>" id= "submit%=i%>">Submit</button>
			</form> -->
		<%}%>
</body>
	<script src="http://localhost:3000/main.js"></script>
<html>